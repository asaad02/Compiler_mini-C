int main() {
    int a ;
    int b ;
    int x ;
    int y ;
    int z ;
    int w ;
    int p[10];
    a + b / (a - 5);

    // check != and == operators and || and && operators and ! operator and < and > operators and <= and >= operators
    if (a != b  ) {
        return 0;
    }

    if (a != b) {
        return 0;
    }

    if (a < b  ) {
        return 0;
    }

    if (a > b  ) {
        return 0;
    }

    if (a <= b  ) {
        return 0;
    }

    if (a >= b  ) {
        return 0;
    }

    if (a == b ) {
        return 0;
    }


}